
name: Update Sunbay Weather Card

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Dependencies
        run: |
          npm install canvas node-fetch@2

      - name: Generate Weather Card
        run: |
          cat > generate.cjs << 'EOF'
          // (code block unchanged — your generator lives here)
          EOF

          node generate.cjs

      - name: Commit & Push
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action"
          git add sunbay_marina_forecast.png
          git diff --staged --quiet || git commit -m "Weather card update"
          git push
