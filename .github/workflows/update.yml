name: Update Sunbay Weather Card
on:
  schedule:
    - cron: '0 */4 * * *'  # Every 4 hours
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger PNG Generation
        run: curl -s https://capt-ks.github.io/sunbay-weather/ > /dev/null
